# .env.development - Development Environment Configuration
# Copy this file to .env.development and customize for your development setup
# This file can be committed to version control

# Transmission RPC Configuration
TRANSMISSION_HOST=localhost
TRANSMISSION_PORT=9091
TRANSMISSION_USER=development_user
TRANSMISSION_PASS=development_password

# Development-specific settings
# Set to 'DEBUG' for verbose logging during development
LOG_LEVEL=DEBUG

# Logging (optional)
# Uncomment to log to a file during development
# LOG_FILE=~/logs/iso-updater-dev.log

# Distributions to update during development
# Options: centos, debian, ubuntu, arch, raspberrypi, mint, fedora, popos, rocky, alma, manjaro, elementary, zorin, endeavour
# Only test with smaller, faster distributions during development
DISTROS=debian,ubuntu,raspberrypi

# Optional: Use a test Transmission instance
# TRANSMISSION_HOST=192.168.1.100
# TRANSMISSION_PORT=9092

# Web Interface Configuration
# Enable/disable the web interface (Flask server on port 8084)
# WEB_ENABLED=true            # Enable for testing web interface
# WEB_ENABLED=false           # Disable for CLI-only testing (default)
WEB_ENABLED=false

# Automatic Scheduling Configuration
# Enable/disable automatic torrent checks
# NOTE: Only works when WEB_ENABLED=true (requires web interface to run)
# SCHEDULE_ENABLED=false           # Disable in development (recommended)
# SCHEDULE_ENABLED=true            # Enable for testing
SCHEDULE_ENABLED=false

# Frequency for automatic checks
# Options: 1h, 8h, 1d, 7d, 14d, 30d
# SCHEDULE_FREQUENCY=1h            # Test with hourly checks
SCHEDULE_FREQUENCY=1d

# Time to run automatic checks (24-hour format HH:MM)
# Only used when SCHEDULE_FREQUENCY is set to 1d or longer
# SCHEDULE_TIME=09:00              # Or set to convenient dev time
SCHEDULE_TIME=02:00

# Default selected distributions in web interface
# SELECT_DISTROS=debian,ubuntu,fedora,mint
